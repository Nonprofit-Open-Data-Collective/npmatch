% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/canonicalize.R
\name{np_sam_review_fields}
\alias{np_sam_review_fields}
\title{SAM context fields for the review queue}
\usage{
np_sam_review_fields(sam)
}
\arguments{
\item{sam}{The raw SAM data frame.}
}
\value{
A data frame keyed by \code{uei} with \code{SAM_}-prefixed context fields.
}
\description{
Builds descriptive fields imported from the SAM / USASpending source record,
\code{SAM_}-prefixed so they read as distinct from the derived \verb{_uss} fields.
Handles either the uppercase-spaced or lowercase-underscore SAM column
conventions. The business-type string is tidied into one boolean per observed
type (\verb{SAM_bus_type_<label>}), and \code{entity_structure} gains a decoded label.
}
\details{
Note: SAM's org-size metrics (the "SAM Numerics" employee/receipt codes) are
not present in the V2 public monthly extract, so no size field is derived
here; use the BMF \code{BMF_assets} / \code{BMF_revenue} as the size proxy.
}
