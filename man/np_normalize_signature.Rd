% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signature.R
\name{np_normalize_signature}
\alias{np_normalize_signature}
\title{Fingerprint the normalizer}
\usage{
np_normalize_signature(detail = FALSE)
}
\arguments{
\item{detail}{If \code{TRUE}, also return the per-component hashes, so a mismatch
can be localized to the rule table or function that moved. Default \code{FALSE}.}
}
\value{
A signature string such as \code{"v1-a1b2c3d4e5f60718"}. With
\code{detail = TRUE}, an \code{np_signature} object carrying that string, the
per-component hashes, and the npmatch version.
}
\description{
A hash over everything a caller may legitimately \strong{precompute and cache}: the
rule tables and functions behind \code{\link[=np_normalize]{np_normalize()}}, the corpus statistics
(\code{\link[=np_name_freq]{np_name_freq()}}, \code{\link[=np_token_idf]{np_token_idf()}}, \code{\link[=np_stopwords]{np_stopwords()}}), and the tokenizer
behind \code{\link[=np_ref_index]{np_ref_index()}}.
}
\section{Why}{
A cached reference stores derived columns — \code{name_key}, \code{street_key}, \code{zip9}
— while the query side is normalized at run time by whatever npmatch is
installed. Change the normalizer between those two moments and the sides are
normalized differently: matches quietly stop being found, with no error and a
symptom that reads as a data problem. Stamp this value into an artifact when
you build it, compare on load, and \strong{error} on mismatch.
}

\section{What is covered}{
Only what is precomputable. Comparison, scoring, vetoes, selection and
tiering are excluded by design — none of them is cached, so a change there
must not invalidate an artifact. A signature that fires on unrelated edits is
one people learn to ignore.

Function bodies are compared after \code{\link[utils:removeSource]{utils::removeSource()}}, so \strong{comments and
reformatting leave the signature unchanged while logic changes move it}.
}

\section{Stability}{
The returned string carries a \verb{v1-} format prefix. If a later npmatch changes
\emph{what} is hashed, the prefix changes too, so that is distinguishable from a
change to the normalizer itself.
}

\examples{
sig <- np_normalize_signature()
sig

# which components make it up
names(np_normalize_signature(detail = TRUE)$components)
}
\seealso{
\code{\link[=np_normalize]{np_normalize()}}
}
