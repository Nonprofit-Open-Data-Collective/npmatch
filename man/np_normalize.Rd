% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize.R
\name{np_normalize}
\alias{np_normalize}
\title{Normalize a query or reference frame}
\usage{
np_normalize(data)
}
\arguments{
\item{data}{An \code{np_query} or \code{np_reference} frame from \code{\link[=np_query]{np_query()}} /
\code{\link[=np_reference]{np_reference()}}.}
}
\value{
The same frame with the normalization columns added.
}
\description{
Brings entity names and addresses onto a common, matchable form (parity with
the BMF \code{org_name_join} field) and extracts the token-level features the veto
layer needs. Adds columns alongside the canonical ones:
}
\details{
\itemize{
\item \code{name_key}     cleaned, abbreviation-standardized, legal-suffix-stripped
\item \code{dba_key}      the DBA name under the same normalization (for cross-matching)
\item \code{name_full}    cleaned name \emph{with} suffix retained
\item \code{name_form}    detected legal form (INC / LLC / CORP / ...), or \code{NA}
\item \code{name_gen}     generation marker (JR/SR/II/III/...), or \code{NA}
\item \code{name_gen_rank} numeric rank of the generation marker, for conflict tests
\item \code{name_nums}    space-joined embedded digit tokens (chapter/local numbers)
\item \code{name_ord}     canonical ordinal rank(s) found in the name
\item \code{street_key}   USPS-standardized street with unit stripped
\item \code{street_num}   leading house / box number
\item \code{street_name}  street body with the number, type, and unit removed
\item \code{street_type}  trailing street type (ST / AVE / BLVD / WAY / ...), or \code{NA}
\item \code{street_unit}  extracted suite/unit/apt number, if any
\item \code{is_po_box}    \code{TRUE} if the street is a PO box (any variant), else \code{FALSE}
\item \code{zip5}         five-digit ZIP, leading zeros restored (nested prefix)
\item \code{zip3}         three-digit ZIP prefix / SCF region (nested prefix)
\item \code{zip9}         full \code{xxxxx-xxxx} ZIP when the +4 is known, else \code{NA}
}

The ZIP match hierarchy is the nested prefix family \code{zip3} \eqn{\subset}
\code{zip5} \eqn{\subset} \code{zip9}. The +4 add-on (\code{zip_plus4}) is an input only,
consumed to build \code{zip9}; it is not a standalone level.
\itemize{
\item \code{state_abb}    two-letter state code (mapped from a full state name if given)
}
}
