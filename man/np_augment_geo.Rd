% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/augment.R
\name{np_augment_geo}
\alias{np_augment_geo}
\title{Augment records with ZIP-level geography}
\usage{
np_augment_geo(data, crosswalk, by = "zip5", cols = NULL)
}
\arguments{
\item{data}{A data frame with a ZIP key column (default \code{zip5}).}

\item{crosswalk}{A data frame keyed by the same column.}

\item{by}{The join key column name present in both. Default \code{"zip5"}.}

\item{cols}{Columns from \code{crosswalk} to add; default all non-key columns.}
}
\value{
\code{data} with the crosswalk columns joined on.
}
\description{
Left-joins a ZIP-keyed crosswalk onto \code{data}, adding whatever columns it
carries (county, CBSA/metro name, RUCA code, urban/rural flag, ...). npmatch
ships no crosswalk data; you supply one built from a standard source and load
it yourself:
}
\details{
\itemize{
\item \strong{county / CBSA (metro):} HUD USPS ZIP Crosswalk (ZIP->CBSA, ZIP->county)
or the \code{zipcodeR} package's \code{zip_code_db}.
\item \strong{urban / rural:} USDA ERS ZIP-code RUCA codes (then \code{\link[=np_ruca_urban]{np_ruca_urban()}}).
}

The crosswalk must have a \code{zip5} (or \code{by}) key column; all other columns are
added to \code{data}. Join first on the record's own \code{zip5} from \code{\link[=np_normalize]{np_normalize()}}.
}
\examples{
\dontrun{
# from the zipcodeR package
xwalk <- zipcodeR::zip_code_db[, c("zipcode", "county", "state")]
names(xwalk)[1] <- "zip5"
records <- np_augment_geo(records, xwalk)
}

}
