% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch.R
\name{np_batch}
\alias{np_batch}
\title{Split a data frame into batches}
\usage{
np_batch(x, size = 2500L, n = NULL, shuffle = TRUE, seed = NULL)
}
\arguments{
\item{x}{A data frame.}

\item{size}{Target rows per batch. Ignored if \code{n} is supplied. Default 2500 —
at the observed ~8\% review rate this yields ~200 review cases per batch, one
LLM-validation pass.}

\item{n}{Number of batches. Overrides \code{size} when given.}

\item{shuffle}{Shuffle rows before splitting. Default \code{TRUE}.}

\item{seed}{Optional RNG seed for reproducible shuffling. The global RNG state
is saved and restored, so callers are unaffected.}
}
\value{
A list of data frames (the batches), with attribute \code{n_batches}.
An empty input returns an empty list.
}
\description{
Partitions the rows of a data frame into evenly sized batches for chunked
processing (e.g. feeding a large source through \code{\link[=np_run_batches]{np_run_batches()}} a chunk at
a time). Rows are shuffled first (deterministically, given \code{seed}) so each
batch is a representative slice rather than an accident of source ordering.
}
\details{
Batches are made \strong{as even as possible}: with \code{size}, the number of batches
is \code{ceiling(nrow / size)} and each batch holds \code{nrow / n_batches} rows (so a
batch is never larger than \code{size}, and there is no tiny remainder batch); with
\code{n}, the rows are split into exactly \code{n} near-equal batches.
}
\examples{
b <- np_batch(mtcars, size = 10, seed = 1)
length(b); vapply(b, nrow, integer(1))
}
