% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stage3.R
\name{np_stage3_run}
\alias{np_stage3_run}
\title{Run stage 3 - LLM research of the NO cases}
\usage{
np_stage3_run(
  project = np_project_root(),
  sources = c("stage1_no", "stage2_no"),
  source_data = NULL,
  bmf_index = NULL,
  efile_domains = NULL,
  packet_size = 8L,
  rules = np_gate_rules(),
  verbose = TRUE
)
}
\arguments{
\item{project}{Project root. Defaults to \code{\link[=np_project_root]{np_project_root()}}.}

\item{sources}{Which stages' NO files to research. Default both; pass
\code{"stage1_no"} to research only what the matcher rejected outright.}

\item{source_data}{Optional frame of source attributes (the prepared SAM
query) keyed by \code{uei}, to enrich the packets beyond name alone.}

\item{bmf_index}{Optional data frame with an \code{ein} column (and optionally
\code{name}) used both for the escape hatch and to enforce the \code{match}
constraint on collection.}

\item{efile_domains}{Optional character vector of web domains known to belong
to 990 filers, for the escape hatch.}

\item{packet_size}{Cases per research packet. Default 8.}

\item{rules}{Entity-gate rules. Defaults to \code{\link[=np_gate_rules]{np_gate_rules()}}.}

\item{verbose}{Print progress. Default \code{TRUE}.}
}
\value{
Invisibly: the findings once results exist, otherwise the seed frame.
}
\description{
Resumable, like \code{\link[=np_stage2_run]{np_stage2_run()}}, because a research agent sits in the
middle:
}
\details{
\itemize{
\item \strong{First call} — pools the NO cases from the stages named in \code{sources},
applies the \code{\link[=np_entity_gate]{np_entity_gate()}} screen, settles what it can deterministically,
and writes research packets to \verb{03_stage3/batches/packets/}. Point an agent
at \verb{03_stage3/PROMPT.md}; it writes one \verb{run-<NNNN>.tsv} per packet to
\verb{03_stage3/batches/out/}.
\item \strong{Second call} — folds those results onto the seed and writes
\code{stage3_yes}, \code{stage3_no} and \code{stage3_research_findings}.
}
}
\section{What the screen settles for free}{

Foreign registrants, for-profit legal forms, government units and sole
proprietors get a determination and high confidence without any lookup. An
escape hatch overrides the screen where there is contrary evidence — a hit in
\code{bmf_index}, an unreliable SAM 2L flag, or a 990 filer sharing the registrant
website (\code{efile_domains}) — so a seeded case with a reason to doubt it is
still researched.
}

\section{The \code{match} constraint}{

A \code{match} must resolve to an EIN the reference actually contains. An EIN that
is real but absent from this reference vintage is reclassified to
\code{nonprofit_not_in_bmf} with the EIN retained, because a match the crosswalk
cannot join is not a match.
}

\seealso{
\code{\link[=np_entity_gate]{np_entity_gate()}}, \code{\link[=np_stage2_run]{np_stage2_run()}}, \code{\link[=np_final_run]{np_final_run()}}.
}
