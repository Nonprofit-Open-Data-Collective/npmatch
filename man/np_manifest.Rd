% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assets.R
\name{np_manifest}
\alias{np_manifest}
\alias{np_manifest_add}
\title{Record and read the data-asset manifest}
\usage{
np_manifest(root = np_data_root())

np_manifest_add(
  asset,
  tier = c("raw", "normalized", "results"),
  source = NA,
  url = NA,
  path = NULL,
  md5 = NA,
  row_count = NA,
  note = NA,
  root = np_data_root()
)
}
\arguments{
\item{root}{Data root. Defaults to \code{\link[=np_data_root]{np_data_root()}}.}

\item{asset}{Short asset name (also the file's basename), e.g. \code{"bmf-2026-01"}.}

\item{tier}{One of \code{"raw"}, \code{"normalized"}, \code{"results"}.}

\item{source}{Human label of the origin (e.g. \code{"NCCS BMF catalog"}).}

\item{url}{Download / origin URL.}

\item{path}{Optional path to the file, used to auto-fill \code{md5} and \code{row_count}.}

\item{md5, row_count}{Optional explicit checksum / row count (override \code{path}).}

\item{note}{Free-text note.}
}
\value{
\code{np_manifest()} returns the manifest data frame; \code{np_manifest_add()}
returns it invisibly after writing.
}
\description{
The manifest is a small CSV under the data root that records the provenance
of every source and derived asset — what it is, where it came from, when it
was fetched, its checksum and row count. It is what makes a run reproducible:
documentation can point at exact vintages, and a colleague can verify they
hold the same bytes.
}
\details{
\code{np_manifest_add()} appends (or replaces, by \code{asset} name) one row; the \code{md5}
is computed with \code{\link[tools:md5sum]{tools::md5sum()}} when \code{path} is given and \code{md5} is not.
}
