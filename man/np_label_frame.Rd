% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/training.R
\name{np_label_frame}
\alias{np_label_frame}
\title{Build a labelling frame for training and evaluation}
\usage{
np_label_frame(pairs, k = 3)
}
\arguments{
\item{pairs}{Scored + vetoed \code{np_pairs}.}

\item{k}{Number of top-scoring candidates to keep per query. Default 3.}
}
\value{
A data frame (class \code{np_labelframe}) with per-field similarities,
\code{score}, veto info, a \code{candidate_type} (why the pair was kept), and an empty
\code{label} column to be filled in.
}
\description{
Emits candidate pairs in a flat, review-ready form so a human or an LLM can
apply a \code{label} (TRUE match / FALSE non-match). The result feeds two goals:
calibrating tier thresholds, and training the supervised combiner used by
\code{\link[=np_score]{np_score()}} \code{method = "model"}. It is the same schema the refinement hand-off
will use.
}
\details{
To make labelling efficient it keeps, per query, the top-\code{k} scoring pairs
plus the best name-only and best address-only pairs (so the eventual training
set contains hard negatives near the decision boundary, not just easy ones).
}
