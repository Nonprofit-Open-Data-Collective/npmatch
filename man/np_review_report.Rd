% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/review_report.R
\name{np_review_report}
\alias{np_review_report}
\title{Render a review frame as a static HTML report}
\usage{
np_review_report(
  x,
  output = "match-review-report.html",
  decision = "MAYBE",
  max_ueis = 0,
  only_missing_gt = FALSE,
  seed = 42,
  quiet = TRUE
)
}
\arguments{
\item{x}{One of: an \code{np_routing} object from \code{\link[=np_route]{np_route()}} (its \code{review}
element is used); an \code{np_review} frame or plain data frame in the review
schema; or a path to a CSV of one.}

\item{output}{Path for the HTML file to write. Default
\code{"match-review-report.html"} in the working directory. A \code{.html} extension
is added if missing.}

\item{decision}{Which \code{match_decision} tiers to include: \code{"ALL"}, \code{"YES"},
\code{"NO"}, or \code{"MAYBE"} (comma-separate to combine, e.g. \code{"MAYBE,NO"}).
Default \code{"MAYBE"} — the human-review hand-off queue.}

\item{max_ueis}{Cap on the number of UEI sections rendered; \code{0} or \code{NA} means
no cap. When the pool is larger than \code{max_ueis}, a deterministic random
sample (see \code{seed}) is drawn. Default \code{0}.}

\item{only_missing_gt}{Logical; when the frame carries ground-truth columns,
restrict to UEIs whose ground-truth EIN is not among the candidates
(recall misses). Ignored on frames without ground truth. Default \code{FALSE}.}

\item{seed}{Sampling seed used when \code{max_ueis} truncates the pool. Default 42.}

\item{quiet}{Suppress Quarto's rendering progress output. Default \code{TRUE}.}
}
\value{
The path to the written HTML file, invisibly.
}
\description{
Turns the \code{review} queue from \code{\link[=np_route]{np_route()}} into a self-contained HTML report
for human review: one section per source entity (UEI), each candidate laid out
as a side-by-side "matching form" (id, name, nonprofit type, age, size,
affiliate, address, point of contact, and the match-score block). Candidates
are ordered with the pipeline's selected pick first, then by descending
\code{total_score}.
}
\details{
The report is read-only. If the frame also carries evaluation columns
(\code{is_gt_ein}, \code{gt_confidence}, \code{gt_outcome_class}, \code{labeled_by}) — as produced
by the benchmarking harness — the ground-truth candidate is highlighted and
floated to the top of its section, and UEIs whose true EIN is missing from the
candidate set are flagged. On an ordinary \code{\link[=np_route]{np_route()}} frame those columns are
simply absent and the report renders without them.

Rendering requires the \pkg{quarto} package and a Quarto CLI installation (the
same dependency the vignettes use). The bundled template lives at
\code{system.file("report", "match-review-report.qmd", package = "npmatch")}.
}
\examples{
\dontrun{
res     <- np_cascade(query, reference)
routing <- np_route(res, bmf = bmf_raw, sam = sam_raw)
np_review_report(routing, output = "review.html")

# just the recall misses on a labelled evaluation frame
np_review_report("EVAL-FRAME-MERGED.csv", decision = "ALL",
                 only_missing_gt = TRUE, output = "misses.html")
}
}
\seealso{
\code{\link[=np_route]{np_route()}} for producing the review frame; \code{\link[=np_run_report]{np_run_report()}} for a
Markdown run summary.
}
