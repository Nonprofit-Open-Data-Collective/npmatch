% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate.R
\name{np_calibration_frame}
\alias{np_calibration_frame}
\title{Build a labelling frame for threshold / weight calibration}
\usage{
np_calibration_frame(
  tiered,
  per_tier = c(YES = 120, MAYBE = 250, NO = 120),
  k = 3,
  seed = 1
)
}
\arguments{
\item{tiered}{An \code{np_tiered} result from \code{\link[=np_match]{np_match()}} / \code{\link[=np_cascade]{np_cascade()}} (its
scored candidate pairs must be attached as \code{attr(tiered, "pairs")}).}

\item{per_tier}{Named integer cap of queries to sample from each tier.}

\item{k}{Candidates to surface per query. Default 3.}

\item{seed}{Sampling seed.}
}
\value{
A data frame (class \code{np_calibration}) of candidate rows with an empty
\code{label} column.
}
\description{
Draws a tier-stratified sample of queries from a tiered result and lays out
their candidate matches (top-k by score plus the best name-only and best
address-only views) as one row per candidate, ready to label. Sampling across
YES / MAYBE / NO — not just the review pile — is what lets
\code{\link[=np_tune_thresholds]{np_tune_thresholds()}} estimate the cutoffs well near the boundary.
}
\details{
Each row carries both records' display fields, the per-field similarities, the
combined \code{score}, the query's tier / pass / margin, whether the candidate was
the chosen overall match, and an empty \code{label} column. A labeller (human or
LLM) sets \code{label = TRUE} on the correct candidate for a query and \code{FALSE} on
the rest (all \code{FALSE} if none is correct). Feed the result back with
\code{\link[=np_label]{np_label()}}, then \code{\link[=np_benchmark]{np_benchmark()}} / \code{\link[=np_tune_thresholds]{np_tune_thresholds()}}.
}
