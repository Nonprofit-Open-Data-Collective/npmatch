% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select.R
\name{np_select}
\alias{np_select}
\title{Select best matches per query record}
\usage{
np_select(pairs, config = np_config(), include_vetoed = FALSE)
}
\arguments{
\item{pairs}{Scored + vetoed \code{np_pairs} (see \code{\link[=np_score]{np_score()}}, \code{\link[=np_veto]{np_veto()}}).}

\item{include_vetoed}{If \code{TRUE}, vetoed pairs remain eligible (recorded, not
dropped). Default \code{FALSE}.}
}
\value{
A one-row-per-query data frame of class \code{np_selection} with the chosen
\verb{overall_*} match and the alternative \verb{name_*} / \verb{addr_*} matches.
}
\description{
Reduces scored, vetoed candidate pairs to the best reference entity per query
record, and — because the useful MAYBE/NO review set is wider than a single
best guess — also surfaces the best match under two \emph{loosened} views:
}
\details{
\itemize{
\item \code{overall} best by combined \code{score} (the primary decision),
\item \code{name}    best by name similarity alone (address ignored beyond blocking),
\item \code{addr}    best by address similarity alone (name ignored).
}

When the three views disagree, that disagreement is itself signal for review.
Vetoed pairs are excluded from every "best" pick by default.
}
