% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{np_compare}
\alias{np_compare}
\title{Generate and compare candidate pairs}
\usage{
np_compare(
  query,
  reference,
  config = np_config(),
  block = "state",
  profile = NULL,
  candidates = NULL,
  name_freq = NULL,
  token_idf = NULL
)
}
\arguments{
\item{query, reference}{Normalized \code{np_query} / \code{np_reference} frames.}

\item{config}{An \code{\link[=np_config]{np_config()}}.}

\item{block}{Character vector of canonical fields to block on when
\code{candidates} is not supplied. Defaults to \code{"state"}.}

\item{profile}{Optional profile name to force; otherwise \code{\link[=np_detect_geo]{np_detect_geo()}}.}

\item{candidates}{Optional \code{np_blocks} from \code{\link[=np_block]{np_block()}}. When given, these
candidate pairs are compared instead of blocking on \code{block} — the scalable
path (name-token / progressive blocking).}
}
\value{
A data frame of candidate pairs, one row per (query, reference) pair,
with class \code{np_pairs}. Columns include \code{.x}, \code{.y}, \code{.id}, \code{.ein}, a
similarity column per compared field, plus carried veto features.
}
\description{
Blocks the query against the reference, then scores each candidate pair on
the fields implied by the detected (or supplied) geographic profile. reclin2
does the blocked pairing and per-field string comparison; the result is
returned as a plain data frame so every downstream npmatch stage works on
ordinary columns.
}
\details{
The name is always compared (on \code{name_key}). Geographic comparison fields are
taken from the profile. Name-feature columns needed by \code{\link[=np_veto]{np_veto()}} are carried
through for both sides with \verb{_x} / \verb{_y} suffixes.
}
