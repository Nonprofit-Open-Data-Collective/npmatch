% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assets.R
\name{np_data}
\alias{np_data}
\alias{np_data_root}
\alias{np_data_path}
\alias{np_data_init}
\title{npmatch data asset directories}
\usage{
np_data_root()

np_data_path(subdir = "", file = NULL, create = FALSE)

np_data_init(root = np_data_root(), quiet = FALSE)
}
\arguments{
\item{subdir}{One of \code{"raw"}, \code{"normalized"}, \code{"results"}, or \code{""} (the root).}

\item{file}{Optional file name to append to the resolved directory.}

\item{create}{Create the directory if it does not exist. Default \code{FALSE}.}

\item{root}{Data root directory. Defaults to \code{\link[=np_data_root]{np_data_root()}}.}

\item{quiet}{Suppress the informational message. Default \code{FALSE}.}
}
\value{
\code{np_data_root()} and \code{np_data_path()} return a path string;
\code{np_data_init()} returns the root invisibly.
}
\description{
npmatch keeps data outside the package (the reference files are large and
versioned separately). These helpers resolve and create a consistent on-disk
layout with three tiers:
}
\details{
\itemize{
\item \verb{raw/} — immutable source downloads (BMF, SAM extracts), one per vintage.
\item \verb{normalized/} — reusable derived assets built once per source vintage
(the normalized reference, the token-IDF table, the name-frequency table).
\item \verb{results/} — per-run outputs (crosswalks, evaluation frames, run reports).
}

The root is resolved in order from \code{getOption("npmatch.data")}, the
\code{NPMATCH_DATA} environment variable, then a default of \verb{~/npmatch-data}.
}
\examples{
\dontrun{
options(npmatch.data = tempfile("npdata"))
np_data_init()
np_data_path("normalized", "reference-2026-01.rds")
}
}
