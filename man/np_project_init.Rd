% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{np_project_init}
\alias{np_project_init}
\title{Create a run project directory}
\usage{
np_project_init(
  path,
  run_id = NULL,
  bmf = NULL,
  sam = NULL,
  config = np_config(),
  templates = TRUE,
  use = TRUE,
  overwrite = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{path}{Directory to create for this run.}

\item{run_id}{Short run label recorded in reports and in the \code{run_id} column
of every stage output. Defaults to \code{basename(path)}.}

\item{bmf}{Optional path to (or vintage tag of) the reference used by this
run, recorded in \verb{00_bmf/SOURCE.md}. \code{NULL} leaves a placeholder.}

\item{sam}{Optional path to a SAM extract to record in \verb{00_sams/README.md}.
The file is not copied.}

\item{config}{An \code{\link[=np_config]{np_config()}} whose thresholds and weights are serialized to
\code{config.yml}.}

\item{templates}{Write the README / PROMPT / AGENTS documents. Default \code{TRUE}.}

\item{use}{Set this project as the session default. Default \code{TRUE}.}

\item{overwrite}{Overwrite existing template documents. Directories and data
files are never removed. Default \code{FALSE}.}

\item{quiet}{Suppress the summary message. Default \code{FALSE}.}
}
\value{
The project root, invisibly.
}
\description{
Scaffolds one directory per matching run: numbered stage folders, each with
\verb{batches/}, \verb{interim/} and \verb{logs/} subdirectories, a \code{README.md} explaining
what the stage does, and (for the LLM stages) a \code{PROMPT.md} holding the agent
instructions. Writes a \code{config.yml}, an empty \code{manifest.csv}, an \code{AGENTS.md}
describing how to orchestrate the whole run, and a first \code{RUN-STATUS.md}.
}
\details{
All six stage folders are created unconditionally. Deploying a stage is
optional — one that never runs simply leaves its outputs absent, which
\code{\link[=np_project_status]{np_project_status()}} reports as \emph{not run} rather than as an error.
}
\section{Reference data is pointed at, not copied}{

The BMF is ~3.5 GB raw plus a ~470 MB normalized bundle, and is reused across
runs. \verb{00_bmf/} therefore holds only \code{SOURCE.md} (vintage, resolved path in
the shared store, checksum, row count) and \code{SIGNATURE.txt} (the
\code{\link[=np_normalize_signature]{np_normalize_signature()}} hash of the cached artifact, so drift is
detectable). The bytes stay in the \code{\link[=np_data_path]{np_data_path()}} store. SAM is genuinely
per-run, so \verb{00_sams/raw/} holds the real extract — paste one in, or let
\code{\link[=np_fetch_sam]{np_fetch_sam()}} download into it.
}

\examples{
p <- file.path(tempdir(), "run-demo")
np_project_init(p, run_id = "demo", quiet = TRUE, use = FALSE)
list.files(p)
unlink(p, recursive = TRUE)
}
\seealso{
\code{\link[=np_project_status]{np_project_status()}} for the run status report, \code{\link[=np_project_path]{np_project_path()}}
for addressing files inside it, \code{\link[=np_data_init]{np_data_init()}} for the shared asset store.
}
