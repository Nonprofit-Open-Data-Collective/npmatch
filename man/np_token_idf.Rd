% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize.R
\name{np_token_idf}
\alias{np_token_idf}
\title{Per-token inverse document frequency for a reference corpus}
\usage{
np_token_idf(name_keys, stopwords = np_stopwords(), min_token_len = 2L)
}
\arguments{
\item{name_keys}{Character vector of normalized names (reference \code{name_key}).}

\item{stopwords}{Tokens to drop (see \code{\link[=np_stopwords]{np_stopwords()}}).}

\item{min_token_len}{Minimum token length to count. Default 2.}
}
\value{
A named numeric vector mapping token -> IDF.
}
\description{
Document frequency of each name token across the reference, turned into
\code{idf = log(N / df)}: rare tokens (distinctive, high IDF) are the ones the
name-token blocking leverages; corpus-common tokens (low IDF) are not. Feed
the result to \code{\link[=np_route]{np_route()}} (\verb{token_idf =}) to annotate the tokenized name
fields with each token's rarity, exposing which tokens carry the match.
}
