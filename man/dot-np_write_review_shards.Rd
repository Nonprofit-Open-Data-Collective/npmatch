% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch.R
\name{.np_write_review_shards}
\alias{.np_write_review_shards}
\title{Run stage-1 matching in batches against a shared reference}
\usage{
.np_write_review_shards(review, base, review_size, id_col = "uei")
}
\arguments{
\item{review_size}{\strong{Review-file} size: the number of MAYBE queries written per
review shard (\code{review-NN-partKK.csv}), sized to one LLM-validation job.
Default 250. Decouples the LLM hand-off size from the compute chunk size.}

\item{query}{Raw source data frame (e.g. a SAM nonprofit subset). Raw
uppercase SAM headers are normalized to the names \code{\link[=np_map_sam]{np_map_sam()}} expects; an
\code{np_query} is used as-is.}

\item{reference}{The reference BMF: a raw data frame, an \code{\link[=np_reference]{np_reference()}}, or a
pre-normalized reference (the latter two skip the corresponding step).}

\item{compute_size}{Queries per \strong{compute chunk} — one \code{\link[=np_cascade]{np_cascade()}} call.
This is the memory-bound knob (candidate pairs scale with chunk size); make
it as large as memory allows to amortize the per-chunk pass machinery.
\code{NULL} (default) runs the whole query in a single chunk ("match big").}

\item{size}{Deprecated alias for \code{compute_size} (back-compat). Ignored if
\code{compute_size} is given.}

\item{seed}{Passed to \code{\link[=np_batch]{np_batch()}} to shuffle before chunking.}

\item{out_dir}{Directory for the per-chunk outputs and the run summary. Created
if needed.}

\item{only}{Optional integer vector of compute-chunk numbers to run (e.g. \code{1}
to run just the first chunk as a demo). Default: all chunks.}

\item{config, method}{Passed to \code{\link[=np_cascade]{np_cascade()}}.}

\item{query_map, reference_map}{Schema maps for raw inputs.}

\item{cache}{Optional \code{.rds} path to cache/reuse the normalized reference and
its \code{name_freq}/\code{token_idf} tables and blocking \code{ref_index}.}

\item{sam_context}{Optional raw SAM frame passed to \code{\link[=np_route]{np_route()}} (\verb{sam =}) to
add \code{SAM_}-prefixed context columns to the review queue.}

\item{threads}{data.table thread count for the blocking joins / reads. Default
\code{NULL} uses all detected cores for the run and restores the prior setting on
exit; pass an integer to pin it, or \code{0} to leave the global setting untouched.}

\item{verbose}{Print progress. Default \code{TRUE}.}
}
\description{
Drives the full stage-1 cascade (\code{\link[=np_cascade]{np_cascade()}}) over a large source in
\code{\link[=np_batch]{np_batch()}} chunks, normalizing the reference \strong{once} and reusing it (plus
the \code{name_freq} / \code{token_idf} tables) for every chunk — turning the ~one-time
reference normalization into exactly that. For each batch it writes an accepted
crosswalk (YES picks), a review queue (the MAYBE hand-off for LLM/human
validation, from \code{\link[=np_route]{np_route()}}), and a short per-batch report; it accumulates a
run summary across batches.
}
\details{
The reference is normalized once via \code{\link[=np_reference]{np_reference()}} + \code{\link[=np_normalize]{np_normalize()}} (or
reused if already normalized). When \code{cache} is a path, the normalized
reference and its \code{name_freq}/\code{token_idf} tables are saved there and reloaded
on the next run, so a resumed or re-run job skips normalization entirely.
}
