% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/block.R
\name{np_ref_index}
\alias{np_ref_index}
\title{Precompute the reference-side token index for blocking}
\usage{
np_ref_index(
  reference,
  token_col = "name_key",
  stopwords = np_stopwords(),
  min_token_len = 2L
)
}
\arguments{
\item{reference}{A normalized \code{np_reference} (from \code{\link[=np_normalize]{np_normalize()}}).}

\item{token_col}{Column to tokenize. Default \code{"name_key"}.}

\item{stopwords}{Tokens to drop (see \code{\link[=np_stopwords]{np_stopwords()}}).}

\item{min_token_len}{Minimum token length to index. Default 2.}
}
\value{
An \code{np_ref_index} object. It is only reused by \code{\link[=np_block]{np_block()}} when
\code{token_col}, \code{stopwords}, and \code{min_token_len} match the block call.
}
\description{
Tokenizes the reference \code{token_col} (default \code{name_key}) once — the inverted
index plus per-token document frequency that \code{\link[=np_block]{np_block()}} otherwise rebuilds on
every token pass. Pass the result to \code{\link[=np_block]{np_block()}} / \code{\link[=np_cascade]{np_cascade()}} /
\code{\link[=np_run_batches]{np_run_batches()}} via \verb{ref_index =} to skip re-tokenizing a large reference on
every pass and every batch. The blocking results are identical either way.
}
\details{
Both base tokens and adjacent-token concatenations (the \code{concat_adjacent}
bigrams) are indexed, so one index serves passes with or without
\code{concat_adjacent}.
}
