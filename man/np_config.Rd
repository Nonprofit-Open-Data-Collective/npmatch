% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{np_config}
\alias{np_config}
\title{Configuration for a linkage run}
\usage{
np_config(
  weights = np_default_weights(),
  thresholds = c(yes = 0.78, maybe = 0.65),
  min_margin = 0.05,
  tie_band = 0.05,
  tie_high = 0.95,
  tiebreak = "name",
  distinct_name_maxfreq = 3,
  distinct_name_floor = 0.9,
  distinct_name_floor_xstate = 0.72,
  overlap_maybe_floor = 0.8,
  name_comparator = "jaro_winkler",
  addr_comparator = "jaro_winkler",
  jw_threshold = 0.85,
  rules = np_default_rules(),
  geo_weights = np_default_geo_weights(),
  name_geo = c(name = 0.6, geo = 0.4)
)
}
\arguments{
\item{weights}{Named list mapping geo-profile name to a named numeric vector
of field weights. Fields absent from a profile are ignored.}

\item{thresholds}{Named numeric of length 2, \code{yes} and \code{maybe}, on the
0-1 score scale. \code{score >= yes} -> YES; \verb{>= maybe} -> MAYBE; else NO.}

\item{min_margin}{Minimum lead the best match must have over the runner-up
(next-best distinct entity) to be auto-accepted. A near-tie (\code{margin < min_margin}) is held at MAYBE for disambiguation even above \code{yes}.}

\item{tie_band}{Score window below the top candidate within which other
candidates count as "tied" (used to flag 2-3 close candidates).}

\item{tie_high}{When tied candidates are all at or above this score, the tie
is broken by the \code{tiebreak} rule instead of the combined score.}

\item{tiebreak}{How to break a high tie: \code{"name"} picks the tied candidate
with the highest name similarity (address is uninformative when several
strong candidates exist); \code{"none"} keeps the combined-score pick.}

\item{distinct_name_maxfreq, distinct_name_floor}{Distinctive-name promotion
(hier scoring): an exact name match on a name shared by at most
\code{distinct_name_maxfreq} reference records is trusted even without address
corroboration, its score floored up to \code{distinct_name_floor}.}

\item{distinct_name_floor_xstate}{Lower promotion floor applied when the
distinctive-name match is \emph{not} state-confirmed (query and candidate in
different states, or no state to compare). Cross-state exact-name-only
matches are far less reliable than same-state ones, so they are floored to
this lower value (landing in MAYBE for review) rather than \code{distinct_name_floor}.}

\item{overlap_maybe_floor}{Minimum token-overlap name score for a
containment/acronym recovery match to be floored to the MAYBE tier. Such
matches (found only via token overlap, not JW/DBA) are always capped below
YES -- containment cannot separate a parent from a subsidiary or chapter --
so they surface for review rather than auto-accepting.}

\item{name_comparator, addr_comparator}{reclin2 comparator constructors used
for name-like and address-like fields respectively.}

\item{jw_threshold}{Jaro-Winkler floor passed to the comparators; per-field
similarities below this are set to 0 (reduces noise from unrelated pairs).}

\item{rules}{A veto rule set as returned by \code{\link[=np_default_rules]{np_default_rules()}} (or a
user-extended version).}

\item{geo_weights}{Named numeric of location-granularity credits used by
\code{np_score(method = "hier")} (see \code{\link[=np_default_geo_weights]{np_default_geo_weights()}}).}

\item{name_geo}{Named numeric \code{c(name=, geo=)} splitting the hierarchical
score between the name and the geo sub-score. Should sum to 1.}
}
\value{
An object of class \code{np_config}.
}
\description{
Bundles the comparators, per-field weights, tier thresholds, and active veto
rule set so a run is reproducible and tunable. Weights are supplied per geo
profile; \code{\link[=np_detect_geo]{np_detect_geo()}} picks the profile at run time and \code{\link[=np_score]{np_score()}}
uses the matching weight vector. Weights need not sum to 1 — they are
normalised internally.
}
