% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score.R
\name{np_score}
\alias{np_score}
\title{Score candidate pairs}
\usage{
np_score(
  pairs,
  config = np_config(),
  method = c("weighted", "hier", "em", "model"),
  model = NULL
)
}
\arguments{
\item{pairs}{An \code{np_pairs} frame from \code{\link[=np_compare]{np_compare()}}.}

\item{config}{An \code{\link[=np_config]{np_config()}}.}

\item{method}{One of \code{"weighted"}, \code{"em"}, \code{"model"}.}

\item{model}{A fitted model with a \code{predict()} method, required for
\code{method = "model"}. Must accept a data frame of the similarity columns.}
}
\value{
\code{pairs} with a numeric \code{score} column added (and method raw output).
}
\description{
Collapses the per-field similarities of an \code{\link[=np_compare]{np_compare()}} result into a single
\code{score} in [0, 1]. Three methods are provided so the "best" combiner can be
chosen empirically against a labelled training set:
}
\details{
\itemize{
\item \code{"weighted"} — normalized weighted sum using the profile weights in
\code{config}. Transparent, no training required. The default.
\item \code{"hier"} — name similarity plus a \emph{hierarchical} geo sub-score that takes
the strongest confirmed location granularity (ZIP+4 > street > ZIP5 > PO boxZIP3 > city > state) via a max, not a sum. Correlated address fields arenot double-counted, and a wrong city cannot pull down a ZIP-confirmed match.Degrades gracefully as geo fields go missing. See np_default_geo_weights().
\item \code{"em"} — unsupervised Fellegi-Sunter weights via \code{\link[reclin2:problink_em]{reclin2::problink_em()}};
\code{score} is the match posterior. Learns weights from the data.
\item \code{"model"} — apply a fitted classifier (\code{glm}, \code{ranger}, \code{xgboost}, ...)
supplied in \code{model} to the per-field similarity columns; \code{score} is its
predicted match probability. This is the hook for supervised optimization.
}

The chosen method's raw output is retained (\code{weight_em}, \code{p_model}) so methods
can be compared side by side before one is adopted.
}
