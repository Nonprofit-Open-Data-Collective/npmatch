% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/route.R
\name{np_route}
\alias{np_route}
\title{Route a tiered result to accept / review / unmatched}
\usage{
np_route(
  tiered,
  config = attr(tiered, "config"),
  pairs = attr(tiered, "pairs"),
  k = 3,
  source = "uss",
  reference = "bmf",
  id_label = "uei",
  bmf = NULL,
  review_tiers = c("YES", "MAYBE", "NO"),
  source_data = NULL,
  reference_data = NULL,
  extra_source = NULL,
  extra_reference = NULL,
  source_key = NULL,
  reference_key = "ein"
)
}
\arguments{
\item{tiered}{An \code{np_tiered} result (carries the scored pairs and config).}

\item{config}{An \code{\link[=np_config]{np_config()}} for the tier thresholds. Defaults to the config
the result was tiered with.}

\item{pairs}{The scored candidate pairs. Defaults to those attached by
\code{\link[=np_match]{np_match()}}.}

\item{k}{Candidates to surface per review case. Default 3.}

\item{source}{Suffix for the query-side (source) columns, replacing \verb{_x}.
Default \code{"uss"} (USASpending). The source varies; the reference is the BMF.}

\item{reference}{Suffix for the candidate-side (reference) columns, replacing
\verb{_y}. Default \code{"bmf"}.}

\item{id_label}{Column name for the query key (\code{.id}), which is ambiguous.
Default \code{"uei"} (Unique Entity Identifier). The reference key is \code{ein}.}

\item{bmf}{Optional raw processed BMF. When supplied, the default context
fields from \code{\link[=np_bmf_review_fields]{np_bmf_review_fields()}} (NTEE, 501(c) subsection, foundation
flag, ruling year, assets, revenue, 990 form type) are joined onto each
review row (suffixed \verb{_<reference>}) and placed in the curated front block.}

\item{review_tiers}{Which tiers to include in the \code{review} inspection frame.
Default all three (\code{c("YES","MAYBE","NO")}) so \code{decision} shows the full
YES/MAYBE/NO outcome; set to \code{"MAYBE"} for just the human-review hand-off.}

\item{source_data, reference_data}{Optional data frames to pull extra
passthrough columns from (e.g. award fields from the source, NTEE/ruling
fields from the BMF). Joined by key onto each review row.}

\item{extra_source, extra_reference}{Character vectors of column names to carry
through from \code{source_data} / \code{reference_data}. Appended, suffixed by
\code{source} / \code{reference}, after the generated features.}

\item{source_key, reference_key}{Join keys in \code{source_data} / \code{reference_data}.
Default to \code{id_label} and \code{"ein"} respectively.}
}
\value{
An \code{np_routing} object: a list of \code{accepted}, \code{review}, \code{unmatched},
and a \code{summary} count vector.
}
\description{
Turns an \code{\link[=np_tier]{np_tier()}} / \code{\link[=np_match]{np_match()}} result into the three hand-off products of
the pipeline:
}
\details{
\itemize{
\item \strong{\code{accepted}} the auto-matched crosswalk (YES tier),
\item \strong{\code{review}} a self-contained queue for the MAYBE tier — one row per
surfaced candidate (top-k by score plus the best name-only and best
address-only pairs). Columns lead with a curated decision block —
\verb{<id_label>, ein, name_<source>, name_<reference>, name_sim, addr_sim, score, decision, decision_layer, decision_reason, notes}, then the aligned
address fields and per-field sims — followed by \strong{every} remaining
generated feature/stat, and finally any optional passthrough columns.
\item \strong{\code{unmatched}} the NO tier, each with its best near-miss for reference.
}

The \code{review} queue is the same schema a human spreadsheet and an LLM prompt
both consume (\code{\link[=np_as_prompt]{np_as_prompt()}} renders one case as prompt text). No LLM or
network calls are made.
}
