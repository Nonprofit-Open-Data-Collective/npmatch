% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch.R
\name{np_fetch}
\alias{np_fetch}
\title{Download a data file}
\usage{
np_fetch(
  url,
  dest = NULL,
  overwrite = FALSE,
  manifest = TRUE,
  timeout = 3600,
  method = "auto",
  quiet = FALSE
)
}
\arguments{
\item{url}{File URL.}

\item{dest}{Destination path. Default: \verb{raw/<basename>} under the data root.}

\item{overwrite}{Re-download if the destination already exists. Default \code{FALSE}.}

\item{manifest}{Record the download in the manifest. Default \code{TRUE}.}

\item{timeout}{Download timeout in seconds. Default 3600.}

\item{method}{Download method passed to \code{\link[utils:download.file]{utils::download.file()}}. Default
\code{"auto"} (R picks a working transport); override with \code{"libcurl"},
\code{"curl"}, or \code{"wininet"} if needed.}

\item{quiet}{Suppress progress. Default \code{FALSE}.}
}
\value{
The destination path, invisibly.
}
\description{
Thin wrapper over \code{\link[utils:download.file]{utils::download.file()}} with sensible defaults for large
files (binary mode, long timeout, resumable skip). Records the download in
the data \code{\link[=np_manifest]{np_manifest()}} when \code{manifest = TRUE}.
}
