% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/block.R
\name{np_block}
\alias{np_block}
\title{Generate candidate pairs by blocking}
\usage{
np_block(
  query,
  reference,
  by = "state",
  by_x = NULL,
  by_y = NULL,
  token = TRUE,
  token_col = "name_key",
  stopwords = np_stopwords(),
  min_token_len = 2L,
  max_ref_freq = NULL,
  min_pair_idf = NULL,
  concat_adjacent = FALSE
)
}
\arguments{
\item{query, reference}{Normalized \code{np_query} / \code{np_reference} frames.}

\item{by}{Character vector of exact-match block columns, or \code{NULL}. Applied to
both sides unless \code{by_x} / \code{by_y} override.}

\item{by_x, by_y}{Per-side block columns for cross-column matching (e.g.
\code{by_x = c("state","name_key")}, \code{by_y = c("state","dba_key")} pairs a query
name against a reference DBA). Default to \code{by}. Columns are matched
position-for-position, so the two must be the same length.}

\item{token}{If \code{TRUE} (default), also require a shared \code{token_col} token.}

\item{token_col}{Column to tokenize. Default \code{"name_key"}.}

\item{stopwords}{Tokens to drop (see \code{\link[=np_stopwords]{np_stopwords()}}).}

\item{min_token_len}{Minimum token length to index. Default 2.}

\item{max_ref_freq}{Optional: drop tokens occurring in more than this many
reference records (kills corpus-common words). \code{NULL} = stopwords only.}

\item{min_pair_idf}{Optional IDF-weighted pruning. A single common token
(e.g. "CALIFORNIA", "CHURCH") should not by itself justify a candidate pair,
but a global \code{max_ref_freq} can't see that such a token is concentrated
within one block (state). With \code{min_pair_idf} set, each shared token is
weighted by its inverse document frequency \code{log(N_ref / df)} and a pair is
kept only when the sum of its shared tokens' IDF is at least this value. One
rare token, or several moderately common ones, clears the bar; a lone common
token does not. \code{NULL} (default) disables this. Only used when \code{token}.}

\item{concat_adjacent}{If \code{TRUE}, also index the concatenation of each adjacent
pair of (post-stopword) tokens as an extra token. This recovers de-spacing
differences symmetrically: "STEP FORWARD" emits the compound "STEPFORWARD"
which matches a reference already spelled "STEPFORWARD", and vice versa. The
compounds are rare (high IDF), so with \code{min_pair_idf} set they only justify a
pair when genuinely shared. Default \code{FALSE}. Only used when \code{token}.}
}
\value{
A data frame of candidate pairs with integer columns \code{.x} (query row)
and \code{.y} (reference row), class \code{np_blocks}.
}
\description{
Produces the (query, reference) candidate pairs worth comparing, so
\code{\link[=np_compare]{np_compare()}} never has to score every same-state pair. Three modes:
}
\details{
\itemize{
\item \strong{exact} (\code{token = FALSE}) — pairs that match exactly on all \code{by} columns
(a hash join). E.g. \code{by = "state"} or \code{by = c("state", "zip5")}.
\item \strong{token} (\code{token = TRUE}, default) — pairs that share the exact \code{by} key
\strong{and} at least one distinctive token of \code{token_col} (a name-token inverted
index). This is what makes blocking scale: "ALASKA X" is only paired with
same-state orgs that share a rare token, not all ~40k orgs in the state.
\item \strong{token, cross-key} (\code{by = NULL}, \code{token = TRUE}) — shared token only, no
exact key. The loose recall-recovery pass (finds a same-name org in another
state — the firm-vs-establishment case).
}

Both frames must be normalized (\code{\link[=np_normalize]{np_normalize()}}). Combine passes by
\code{rbind}-ing results and de-duplicating (see \code{\link[=np_block_union]{np_block_union()}}).
}
