% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/final.R
\name{np_final_run}
\alias{np_final_run}
\title{Roll the stages up into the final deliverables}
\usage{
np_final_run(
  project = np_project_root(),
  precedence = c("1", "2", "3"),
  verbose = TRUE
)
}
\arguments{
\item{project}{Project root. Defaults to \code{\link[=np_project_root]{np_project_root()}}.}

\item{precedence}{Order in which stages win when one source id is matched by
more than one (a duplicate registration can be matched in two stages).
Default \code{c("1", "2", "3")} — the earliest, most automatic match wins.}

\item{verbose}{Print progress. Default \code{TRUE}.}
}
\value{
The crosswalk, invisibly.
}
\description{
Binds the per-stage outcome files into the two artifacts the run exists to
produce, and reports the end-to-end funnel.
}
\details{
\itemize{
\item \strong{\code{crosswalk.csv}} — every matched source id, one row each, carrying
\code{decided_by} so a consumer can tell an auto-accepted stage-1 match from one
an adjudicator or a web search produced. This is the only place the word
"crosswalk" is used; elsewhere the linked pairs are \code{matches}.
\item \strong{\code{eval_frame.csv}} — the candidate-level frame: every candidate stage 1
surfaced, annotated with the final answer for its source id.
}
}
\section{Answers found outside the candidate set}{

Stage 3 recovers matches the matcher never surfaced, so the final EIN for a
source id is often absent from \code{stage1_k_candidates}. Those are \strong{not}
dropped: a synthetic row is emitted carrying the final EIN with its
similarity columns empty and \code{candidate_source = "stage3_research"}, and
\code{final_ein_in_candset} records which case each row is.

That flag splits recall loss in two. A match whose EIN was never in the
candidate set is a \strong{blocking} failure — no threshold change recovers it. One
that was surfaced but scored below the floor is a \strong{scoring} failure, and is
recoverable by recalibration. Collapsing them would hide the only number that
says which of the two to work on.
}

\seealso{
\code{\link[=np_stage1_run]{np_stage1_run()}}, \code{\link[=np_stage2_run]{np_stage2_run()}}, \code{\link[=np_stage3_run]{np_stage3_run()}}.
}
