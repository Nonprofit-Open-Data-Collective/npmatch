% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decisions.R
\name{np_merge_decisions}
\alias{np_merge_decisions}
\title{Merge adjudication decisions onto a review frame}
\usage{
np_merge_decisions(
  review,
  decisions,
  id = "uei",
  ein = "ein",
  prefix = "llm_",
  quiet = FALSE
)
}
\arguments{
\item{review}{The candidate-level frame: an \code{np_routing} from \code{\link[=np_route]{np_route()}}
(its \code{review} element is used), a review frame / data frame, or a path to a
CSV of one.}

\item{decisions}{The adjudication result: a data frame, a path to a merged
decisions CSV, or a path to a \strong{directory} of per-shard \verb{decision-*.csv}
files (read and stacked, with a \code{.shard} column recording the source file).
Must carry \code{uei}, \code{best_ein}, \code{llm_decision}, \code{llm_confidence},
\code{llm_reason}.}

\item{id}{Column naming the query key in \code{review}. Default \code{"uei"}; the
decision side is always keyed on \code{uei}. Set this when \code{\link[=np_route]{np_route()}} was
called with a different \code{id_label}.}

\item{ein}{Column naming the candidate key in \code{review}. Default \code{"ein"}.}

\item{prefix}{Prefix for the added columns. Default \code{"llm_"}, giving
\code{llm_decision}, \code{llm_confidence}, \code{llm_reason}, \code{llm_best_ein}, and
\code{llm_selected}. Use e.g. \code{"human_"} to merge a second, independent pass
alongside the first without collision.}

\item{quiet}{Suppress the merge summary message and warnings. Default \code{FALSE}.}
}
\value{
\code{review} with five columns appended, in its original row order and
class. Carries a \code{decision_merge} attribute: a list of \code{n_decisions},
\code{n_ids}, \code{n_collapsed}, \code{n_matched} (review rows that got a decision),
\code{n_unmatched_ids} (decision ids absent from the frame), and \code{conflicts}.
}
\description{
Joins the query-level YES/NO decisions produced by the stage-2 review (LLM
subagents or human reviewers) back onto the candidate-level review frame from
\code{\link[=np_route]{np_route()}}, and derives which candidate the adjudicator actually chose.
}
\details{
\code{\link[=np_route]{np_route()}} makes no LLM or network calls: its \code{review} frame is the hand-off
\emph{into} adjudication, and the decisions come back as a separate artifact. This
is the join that closes that loop.
}
\section{Grain}{

Decisions are \strong{query-level} — one row per source id. The review frame is
\strong{candidate-level} — several rows per source id, one per surfaced EIN. The
decision columns therefore replicate across all of a query's candidate rows,
and the per-row answer is \code{llm_selected}: \code{TRUE} on the single candidate the
adjudicator picked. It is the stage-2 analogue of \code{is_top_candidate}, and
comparing the two shows where adjudication overrode the pipeline's pick.
}

\section{Unadjudicated rows are NA, not FALSE}{

A decision file normally covers the MAYBE tier only, so queries that were
auto-accepted or auto-rejected have no decision row. Their decision columns
and \code{llm_selected} come back \code{NA} — meaning \emph{not adjudicated}, which is not
the same as rejected. Guard with \verb{\%in\% TRUE} (or \code{isTRUE()}) rather than
letting \code{NA} propagate when filtering.
}

\section{Duplicate source registrations}{

A source id matched in more than one compute chunk can be adjudicated in more
than one shard. Those rows are collapsed to one, preferring a YES over a NO
and then higher confidence — the same precedence the run-level merge uses.
Ids whose duplicate rows genuinely disagreed are recorded in the \code{conflicts}
attribute of the result, and warned about unless \code{quiet = TRUE}.
}

\examples{
review <- data.frame(
  uei = c("A1", "A1", "B2", "C3"),
  ein = c("11-1111111", "22-2222222", "33-3333333", "44-4444444"),
  name_uss = c("Alpha", "Alpha", "Beta", "Gamma"),
  stringsAsFactors = FALSE
)
decisions <- data.frame(
  uei            = c("A1", "B2"),
  best_ein       = c("22-2222222", ""),
  llm_decision   = c("YES", "NO"),
  llm_confidence = c("high", "medium"),
  llm_reason     = c("same org; address confirms", "no credible candidate"),
  stringsAsFactors = FALSE
)
merged <- np_merge_decisions(review, decisions)
merged[, c("uei", "ein", "llm_decision", "llm_selected")]
# C3 was never adjudicated -> NA, not FALSE
}
\seealso{
\code{\link[=np_route]{np_route()}} for producing the review frame, \code{\link[=np_as_prompt]{np_as_prompt()}} for
rendering one case for an adjudicator, and \code{\link[=np_evaluate]{np_evaluate()}} for scoring the
merged result against labels.
}
