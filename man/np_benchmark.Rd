% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/train.R
\name{np_benchmark}
\alias{np_benchmark}
\title{Benchmark scoring methods by cross-validation}
\usage{
np_benchmark(
  pairs,
  methods = c("weighted", "em", "model"),
  k = 5,
  config = np_config(),
  engine = "logit",
  target_precision = 0.98,
  min_recall = 0.95,
  seed = 1L
)
}
\arguments{
\item{pairs}{A labelled \code{np_pairs} frame.}

\item{methods}{Which scorers to compare.}

\item{k}{Number of CV folds for the supervised \code{model} method.}

\item{config}{An \code{\link[=np_config]{np_config()}} (supplies weights for \code{weighted}).}

\item{engine}{Training engine for \code{model} (see \code{\link[=np_train]{np_train()}}).}

\item{target_precision, min_recall}{Passed to \code{\link[=np_tune_thresholds]{np_tune_thresholds()}}.}

\item{seed}{Fold-assignment seed.}
}
\value{
A data frame, one row per method, with AUC and tuned-threshold
precision/recall/coverage. Out-of-fold scores are attached as
attribute \code{scores}.
}
\description{
Compares the candidate scorers on a labelled set so the "best" combiner can be
chosen empirically. The unsupervised methods (\code{weighted}, \code{em}) are scored on
the full candidate set; \code{model} uses out-of-fold predictions to avoid optimism.
Each method is then evaluated at thresholds tuned on the same labels, so the
comparison reflects how the tiering would actually behave.
}
