% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/augment.R
\name{np_clean_geo}
\alias{np_clean_geo}
\title{Clean and standardize city and state fields}
\usage{
np_clean_geo(
  data,
  engine = c("rules", "campfin"),
  aliases = np_city_aliases(),
  zip_ref = NULL,
  max_dist = 2
)
}
\arguments{
\item{data}{A data frame with \code{city} / \code{state} (and \code{zip5} for \code{zip_ref}).}

\item{engine}{\code{"rules"} or \code{"campfin"}.}

\item{aliases}{Whole-name city alias map (see \code{\link[=np_city_aliases]{np_city_aliases()}}).}

\item{zip_ref}{Optional \code{zip5}-keyed crosswalk for misspelling correction.}

\item{max_dist}{Max edit distance to treat a city as a correctable near-miss.}
}
\value{
\code{data} with \code{city} / \code{state} cleaned in place.
}
\description{
Normalizes \code{city} and \code{state} in place. Two engines:
}
\details{
\itemize{
\item \code{"rules"} (default, no dependency) — upper-case and squish, expand the
common abbreviated prefixes (\code{ST}->\code{SAINT}, \code{FT}->\code{FORT}, \code{MT}->\code{MOUNT}),
apply whole-name city aliases (\code{NYC}->\verb{NEW YORK CITY}), and coerce \code{state}
to a two-letter code.
\item \code{"campfin"} — delegate city/state normalization to the \code{campfin} package
(\code{normal_city()} / \code{normal_state()}), which validate against USPS and
Census place lists (expanding abbreviations, stripping trailing state
tokens, and nulling known-invalid entries). The alias map is still applied
on top.
}

Correcting arbitrary \emph{misspellings} (e.g. "PHOENex" -> "PHOENIX") needs a
reference of valid place names. Supply \code{zip_ref} (a \code{zip5}-keyed crosswalk
with a city column, e.g. from \code{\link[=np_load_zipcode_db]{np_load_zipcode_db()}}): a missing city is
filled from its ZIP, and a near-miss (edit distance <= \code{max_dist}) is snapped
to the ZIP's canonical city.
}
