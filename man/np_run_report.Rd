% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{np_run_report}
\alias{np_run_report}
\title{Summarise a cascade run as a report}
\usage{
np_run_report(
  res,
  inputs = NULL,
  timings = NULL,
  outputs = NULL,
  reference = NULL,
  file = NULL,
  title = "npmatch run report"
)
}
\arguments{
\item{res}{An \code{np_tiered} result from \code{\link[=np_cascade]{np_cascade()}} (carrying its \code{stages},
\code{pairs}, and \code{config} attributes).}

\item{inputs}{Optional data frame or named list describing the source files
used (e.g. columns \code{file}, \code{vintage}, \code{rows}, \code{md5}). Rendered verbatim.}

\item{timings}{Optional named numeric vector of stage durations in seconds
(e.g. \code{c(normalize = 610, cascade = 2900)}).}

\item{outputs}{Optional character vector of output file paths written.}

\item{reference}{Label for the reference used, e.g. \code{"unified (active+inactive)"}.}

\item{file}{Optional path to write the Markdown report to.}

\item{title}{Report title.}
}
\value{
The Markdown report as a length-one character string (invisibly if
\code{file} is given). A structured list of the computed pieces is attached as
\code{attr(., "parts")}.
}
\description{
Assembles a human-readable report from an \code{\link[=np_cascade]{np_cascade()}} result: provenance
(version, config, inputs), the matching process (per-pass cascade table, how
many candidates were generated, how matches were formed), the outcome
(YES / MAYBE / NO with margins and vetoes), operations (timings, files
written), and a pointer to the review hand-off for the MAYBE queue.
}
\details{
Most of the content is read straight off the result object —
\code{attr(res, "stages")} (the per-pass summary) and \code{attr(res, "pairs")} (the
scored candidate union) — so it works on any cascade result without a
labelled truth set. Supply \code{inputs}, \code{timings}, and \code{outputs} to record
provenance and operational context that the result object cannot know.
}
