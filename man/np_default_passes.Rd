% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cascade.R
\name{np_default_passes}
\alias{np_default_passes}
\title{Default progressive blocking passes}
\usage{
np_default_passes()
}
\value{
A list of pass specifications.
}
\description{
The tight-to-loose blocking cascade used by \code{\link[=np_cascade]{np_cascade()}}. Each pass is a
list with a \code{name}, a human-readable \code{desc} (the decision criterion, shown in
the stage report), and \code{args} passed to \code{\link[=np_block]{np_block()}}. Earlier passes are cheap
and high-precision; later passes are looser and only run on the residual
(queries not yet auto-accepted).
}
\details{
The first tier is four \strong{exact} passes that exploit both the legal name and
the DBA on each side (name==name, name==dba, dba==name, dba==dba), then
name-token blocking within state, then a cross-state token pass for the
firm-vs-establishment case.
}
