% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cascade.R
\name{np_cascade}
\alias{np_cascade}
\title{Match with a progressive blocking cascade}
\usage{
np_cascade(
  query,
  reference,
  config = np_config(),
  method = "hier",
  passes = np_default_passes(),
  verbose = TRUE,
  query_map = np_map_sam(),
  reference_map = np_map_bmf()
)
}
\arguments{
\item{query, reference}{Raw data frames or \code{np_query}/\code{np_reference}.}

\item{config}{An \code{\link[=np_config]{np_config()}}.}

\item{method}{Scoring method for \code{\link[=np_score]{np_score()}} (default \code{"hier"}).}

\item{passes}{A list of pass specs (see \code{\link[=np_default_passes]{np_default_passes()}}).}

\item{verbose}{Print the per-stage report. Default \code{TRUE}.}

\item{query_map, reference_map}{Schema maps for raw inputs.}
}
\value{
An \code{np_tiered} result with a \code{pass} column recording which pass
produced each query's chosen match. A per-stage summary is attached as
\code{attr(result, "stages")}; the scored candidate union as \code{attr(result, "pairs")}.
}
\description{
Runs \code{\link[=np_block]{np_block()}} -> \code{\link[=np_compare]{np_compare()}} -> \code{\link[=np_score]{np_score()}} -> \code{\link[=np_veto]{np_veto()}} pass by
pass, tight to loose. After each pass, queries that reached the YES tier are
removed from the pending set, so each successive (more expensive) pass only
processes the harder residual. Candidate pairs are scored once; the final
\code{\link[=np_select]{np_select()}} / \code{\link[=np_tier]{np_tier()}} run over the de-duplicated union, so a match found
by a looser pass still competes on equal footing (and the runner-up margin is
computed across all of a query's candidates).
}
\details{
With \code{verbose = TRUE} it reports the scoring method and decision criteria
once, then per stage: candidates generated, YES / MAYBE / NO found, how many
were resolved, how many remain pending, and the median YES margin.
}
