% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match.R
\name{np_match}
\alias{np_match}
\title{Run the full linkage pipeline}
\usage{
np_match(
  query,
  reference,
  config = np_config(),
  method = "weighted",
  model = NULL,
  block = "state",
  profile = NULL,
  candidates = NULL,
  query_map = np_map_sam(),
  reference_map = np_map_bmf()
)
}
\arguments{
\item{query}{A raw query data frame, or an \code{np_query} from \code{\link[=np_query]{np_query()}}.}

\item{reference}{A raw reference data frame, or an \code{np_reference}.}

\item{config}{An \code{\link[=np_config]{np_config()}}.}

\item{method}{Scoring method for \code{\link[=np_score]{np_score()}}.}

\item{model}{A fitted \code{\link[=np_train]{np_train()}} model, required when \code{method = "model"}.}

\item{block, profile, candidates}{Passed to \code{\link[=np_compare]{np_compare()}}; supply
\code{candidates} from \code{\link[=np_block]{np_block()}} for the scalable name-token blocking path.}

\item{query_map, reference_map}{Schema maps used only if \code{query}/\code{reference}
are raw data frames.}
}
\value{
An \code{np_tiered} result. The scored candidate pairs are attached as
\code{attr(result, "pairs")}.
}
\description{
Convenience wrapper chaining normalize -> compare -> score -> veto -> select
-> tier. Intermediate objects are attached as attributes for inspection.
}
