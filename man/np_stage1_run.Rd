% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stages.R
\name{np_stage1_run}
\alias{np_stage1_run}
\title{Run stage 1 - the probabilistic cascade}
\usage{
np_stage1_run(
  query = NULL,
  reference = NULL,
  project = np_project_root(),
  cache = NULL,
  compute_size = 2500L,
  review_size = 250L,
  k = 3,
  seed = 1L,
  method = "hier",
  query_map = np_map_sam(),
  reference_map = np_map_bmf(),
  config = np_config(),
  bmf_context = NULL,
  sam_context = NULL,
  only = NULL,
  verbose = TRUE
)
}
\arguments{
\item{query}{The prepared source frame, or a path to one. Defaults to
\verb{00_sams/sam_query.csv} in the project.}

\item{reference}{The reference BMF: raw data frame, \code{\link[=np_reference]{np_reference()}}, or
pre-normalized reference. May be \code{NULL} when \code{cache} holds a built bundle.}

\item{project}{Project root. Defaults to \code{\link[=np_project_root]{np_project_root()}}.}

\item{cache}{Optional \code{.rds} bundle of the normalized reference, passed to
\code{\link[=np_run_batches]{np_run_batches()}}.}

\item{compute_size, review_size, seed, method, config}{Passed to \code{\link[=np_run_batches]{np_run_batches()}}.}

\item{k}{Candidates surfaced per query in \code{stage1_k_candidates}. Default 3.}

\item{query_map, reference_map}{Schema maps for the source and reference,
passed to \code{\link[=np_run_batches]{np_run_batches()}}. Default to \code{\link[=np_map_sam]{np_map_sam()}} / \code{\link[=np_map_bmf]{np_map_bmf()}}.}

\item{bmf_context, sam_context}{Optional raw frames whose \code{BMF_} / \code{SAM_}
context columns are joined onto the candidate frame.}

\item{only}{Optional chunk numbers, for a partial or resumed run.}

\item{verbose}{Print progress. Default \code{TRUE}.}
}
\value{
The consolidated outcome frame, invisibly.
}
\description{
Matches the prepared source against the reference and writes the four stage-1
outputs: \code{stage1_yes}, \code{stage1_maybe}, \code{stage1_no} (all in the shared
\code{\link[=np_stage_schema]{np_stage_schema()}}, one row per source id) and \code{stage1_k_candidates} (one row
per surfaced candidate, joined to any of them by \code{uei}).
}
\details{
Work is delegated to \code{\link[=np_run_batches]{np_run_batches()}}, which normalizes the reference once
and chunks the query. Unlike a bare \code{np_run_batches()} call this runner keeps
everything: the review frame spans \strong{all three tiers}, the NO tier is written
rather than dropped, and each chunk's cascade result and scored pairs are
persisted to \verb{interim/}. Those pairs are the only record of what blocking
surfaced; without them a later evaluation frame cannot be built except by
re-running the match against a possibly-changed candidate set.
}
\seealso{
\code{\link[=np_project_init]{np_project_init()}}, \code{\link[=np_stage2_run]{np_stage2_run()}}, \code{\link[=np_run_batches]{np_run_batches()}}.
}
