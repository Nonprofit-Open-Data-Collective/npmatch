% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{np_project_root}
\alias{np_project_root}
\title{Locate the active npmatch project}
\usage{
np_project_root()
}
\value{
The project root path, or \code{NA_character_} when none is active.
}
\description{
Resolved from \code{getOption("npmatch.project")}, then the \code{NPMATCH_PROJECT}
environment variable. \code{\link[=np_project_init]{np_project_init()}} and \code{\link[=np_project_use]{np_project_use()}} set the
option, so the fetch helpers and stage functions can default their paths into
the run directory without the caller repeating it.
}
\examples{
old <- options(npmatch.project = tempdir())
np_project_root()
options(old)
}
\seealso{
\code{\link[=np_project_use]{np_project_use()}} to point the session at an existing run.
}
