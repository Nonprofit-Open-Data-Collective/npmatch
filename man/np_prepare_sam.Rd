% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch.R
\name{np_prepare_sam}
\alias{np_prepare_sam}
\title{Download-and-prepare a SAM extract for matching}
\usage{
np_prepare_sam(path, out = NULL, codes = c("A8", "BZ", "2U", "A7"))
}
\arguments{
\item{path}{Path to the raw \code{.dat} (or use \code{\link[=np_fetch_sam]{np_fetch_sam()}} first).}

\item{out}{Optional output CSV path. If given, the nonprofit subset is written.}

\item{codes}{Nonprofit codes (see \code{\link[=np_flag_nonprofits]{np_flag_nonprofits()}}).}
}
\value{
The nonprofit subset (invisibly if \code{out} is written).
}
\description{
Reads a raw SAM \code{.dat}, filters to nonprofits, and (optionally) writes the
result. The output is ready to pass to \code{\link[=np_cascade]{np_cascade()}} / \code{\link[=np_match]{np_match()}} with
\code{\link[=np_map_sam]{np_map_sam()}} (after the usual header lower-casing).
}
