% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch.R
\name{np_diff_unmatched}
\alias{np_diff_unmatched}
\title{Retain only source records not already matched (incremental matching)}
\usage{
np_diff_unmatched(
  fresh,
  crosswalk,
  key_fresh = NULL,
  key_cross = NULL,
  status_col = NULL,
  matched_values = c("YES", "MATCH", "match"),
  quiet = FALSE
)
}
\arguments{
\item{fresh}{Fresh source records (data frame) with an id key.}

\item{crosswalk}{Existing crosswalk (data frame) with an id key and, ideally,
a tier/status column.}

\item{key_fresh, key_cross}{Key column names. Auto-detected from
\code{uei} / \verb{UNIQUE ENTITY ID} / \code{unique_entity_id} / \code{.id} if \code{NULL}.}

\item{status_col}{Status/tier column in \code{crosswalk}. Auto-detected from
\code{tier} / \code{match_decision} / \code{status} if \code{NULL}.}

\item{matched_values}{Status values that count as already matched.}

\item{quiet}{Suppress the summary message.}
}
\value{
The retained subset of \code{fresh}, with an integer \code{"diff"} attribute
(\code{fresh}, \code{already_matched}, \code{retained}, \code{new_ids}).
}
\description{
Compares a fresh source table against an existing crosswalk and returns the
subset of fresh records that have \strong{not} already been resolved — so a
re-run only spends compute on new or previously-unmatched organisations.
}
\details{
A record counts as "already matched" if its key appears in the crosswalk
with a status in \code{matched_values} (when a status/tier column is present or
named via \code{status_col}); if no status column is found, any appearance in the
crosswalk counts as matched. Set \code{matched_values = c("YES","MAYBE")} to also
retain (re-run) review-pending cases against a refreshed reference.
}
