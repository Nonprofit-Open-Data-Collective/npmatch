% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stages.R
\name{np_stage2_run}
\alias{np_stage2_run}
\title{Run stage 2 - LLM adjudication of the MAYBE queue}
\usage{
np_stage2_run(
  project = np_project_root(),
  shard_size = 250L,
  columns = .np_slim_cols,
  verbose = TRUE
)
}
\arguments{
\item{project}{Project root. Defaults to \code{\link[=np_project_root]{np_project_root()}}.}

\item{shard_size}{Source ids per adjudication shard. Default 250.}

\item{columns}{Columns carried into the shard files. Defaults to the
adjudication set; pass more to give the agent extra context.}

\item{verbose}{Print progress. Default \code{TRUE}.}
}
\value{
Invisibly: the outcome frame once decisions exist, otherwise the
manifest of shards written.
}
\description{
Resumable, because an adjudicator sits in the middle:
}
\details{
\itemize{
\item \strong{First call} — reads \code{stage1_maybe} and \code{stage1_k_candidates}, writes
shard files to \verb{02_stage2/batches/slim/}, and stops. Point an agent at
\verb{02_stage2/PROMPT.md}; it writes one \verb{decision-<shard>.csv} per shard to
\verb{02_stage2/batches/decisions/}.
\item \strong{Second call} — collects those decisions, collapses duplicate source
registrations, and writes \code{stage2_yes} / \code{stage2_no}.
}

Re-running after only some shards are adjudicated collects what is there and
reports which shards are still outstanding. An unreturned shard is a hole in
the results, not a set of NOs, so it is never silently treated as one.
}
\seealso{
\code{\link[=np_stage1_run]{np_stage1_run()}}, \code{\link[=np_merge_decisions]{np_merge_decisions()}}, \code{\link[=np_stage_schema]{np_stage_schema()}}.
}
