% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate.R
\name{np_tune_thresholds}
\alias{np_tune_thresholds}
\title{Tune YES / MAYBE thresholds from labelled data}
\usage{
np_tune_thresholds(score, label, target_precision = 0.98, min_recall = 0.95)
}
\arguments{
\item{score, label}{Scores and truth on the validation set.}

\item{target_precision}{Minimum precision required of the YES tier.}

\item{min_recall}{Fraction of true matches the MAYBE floor must retain.}
}
\value{
Named numeric \code{c(yes=, maybe=)}, with achieved metrics attached as
attribute \code{metrics}.
}
\description{
Chooses cutoffs for \code{\link[=np_tier]{np_tier()}} on a validation set. \code{yes} is the lowest score
whose auto-accept precision still meets \code{target_precision} (so YES stays
trustworthy while auto-deciding as many records as possible); \code{maybe} is the
lowest score that still captures \code{min_recall} of true matches (anything below
is safe to drop to NO). \code{yes} is never below \code{maybe}.
}
