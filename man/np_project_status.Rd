% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{np_project_status}
\alias{np_project_status}
\title{Report the status of a run project}
\usage{
np_project_status(project = np_project_root(), write = TRUE, quiet = FALSE)
}
\arguments{
\item{project}{Project root. Defaults to \code{\link[=np_project_root]{np_project_root()}}.}

\item{write}{Write \code{RUN-STATUS.md} at the project root. Default \code{TRUE}.}

\item{quiet}{Suppress the printed summary. Default \code{FALSE}.}
}
\value{
A data frame with one row per expected output (\code{stage}, \code{file},
\code{exists}, \code{rows}, \code{modified}), invisibly. The per-stage roll-up is carried
in the \code{stages} attribute.
}
\description{
Scans the project directory and reports, per stage, whether it has run and
what it produced. This is a \strong{scanner, not a ledger}: it reads what is on
disk every time, so it cannot drift out of sync with reality the way a stored
state file would. Deleting an output makes the stage report as incomplete on
the next call.
}
\details{
A stage is \code{complete} when every expected output in the layout is present,
\code{partial} when some are, and \verb{not run} when none are.
}
\examples{
p <- file.path(tempdir(), "run-status-demo")
np_project_init(p, run_id = "demo", quiet = TRUE, use = FALSE)
s <- np_project_status(p, write = FALSE, quiet = TRUE)
attr(s, "stages")[, c("stage", "status")]
unlink(p, recursive = TRUE)
}
\seealso{
\code{\link[=np_project_init]{np_project_init()}}.
}
