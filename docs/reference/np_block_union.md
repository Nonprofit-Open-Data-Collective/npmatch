# Union candidate-pair sets from several blocking passes

Union candidate-pair sets from several blocking passes

## Usage

``` r
np_block_union(...)
```

## Arguments

- ...:

  `np_blocks` objects.

## Value

A de-duplicated `np_blocks`.
