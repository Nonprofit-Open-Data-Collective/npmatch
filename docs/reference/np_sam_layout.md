# SAM public extract column layout

The 142 field names of the SAM Master Extract (Public File V2), in
order, so the headerless pipe-delimited `.dat` can be named.

## Usage

``` r
np_sam_layout()
```

## Value

A character vector of length 142.
